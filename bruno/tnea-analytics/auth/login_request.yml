info:
  name: login_request
  type: http
  seq: 1

http:
  method: POST
  url: "{{backend_url}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier":"StudentThaan",
        "password":"12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
